Install SublimeText         ----                 $  sudo apt-get update
                                                 $  sudo apt-get install sublime-text
                                                 
                                                 
                                                 
Uninstall SublimeText       ----                 $  sudo apt-get remove sublime-text 
                                                 $  sudo apt-get autoremove
                                 
                                 
                                                 
Install Leafpad text editor ----                 $  sudo apt-get install leafpad



Install jupyter  notebook   ----                 $  sudo apt-get install jupyter-notebook
open jupyter notebook       ----                 $  jupyter notebook
close jupyter notebook      ----                 $  jupyter notebook stop



Install sqlite3 command line interface            ----          $  sudo apt-get install sqlite3 libsqlite3-dev

Install sqlite3 browser                           ----          $  sudo apt install sqlitebrowser
open sqlite3 browser                              ----          $  sqlitebrowser


Insatll Avidemux video editor                           ---------  $ sudo add-apt-repository ppa:ubuntuhandbook1/avidemux
                                                                   $ sudo apt-get update
                                                                $ sudo apt-get install avidemux2.7-qt5 avidemux2.7-plugins-qt5
                                                               
Uninsatll Avidemux                                --------------   $ sudo apt-get remove --autoremove avidemux2.7-qt5


Install Octave                            ---------         sudo apt-get install flatpak
                                                            flatpak remote-add --if-not-exists flathub https://flathub.org/repo/flathub.flatpakrepo
                                                            flatpak install flathub org.octave.Octave
                                                            
 Launch Ocatve                            -------            flatpak run org.octave.Octave


Install tesseract on anaconda navigator in ubuntu  ----------  conda install -c jiayi_anaconda pytesseract  


 Install tesseract on anaconda in ubuntu          --------           sudo apt install tesseract-ocr
                                                                sudo apt install libtesseract-dev
                                                                
                                                        
 Install tesseract with pip3 if python 2&3 both are installed               -------               pip3 install pytesseract                                                       
                                                             
                                                             
Install ipywebrtc on conda environment  with any one of the following command  ---------     1) conda install -c conda-forge ipywebrtc 
                                                                                             2) conda install -c conda-forge/label/gcc7 ipywebrtc 
                                                                                             3) conda install -c conda-forge/label/cf201901 ipywebrtc                                                              


Install pygame on conda environment                                  ---------------  conda install -c cogsci pygame


Install 
